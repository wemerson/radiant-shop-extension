.page{:id => "shop_product_#{part[:title].to_slug}", 'data-caption'=>h(part[:title])}
  .part{:id => "shop_product-part-#{part[:title].to_slug}"}
    = render :partial => "admin/shop/products/edit/parts/#{part[:title]}", :locals => { :part => part }