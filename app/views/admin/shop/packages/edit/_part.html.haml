.page{:id => "shop_package_#{part.to_slug}", 'data-caption'=>h(part)}
  .part{:id => "shop_package-part-#{part.to_slug}"}
    = render :partial => "admin/shop/packages/edit/parts/#{part}", :locals => { :part => part }