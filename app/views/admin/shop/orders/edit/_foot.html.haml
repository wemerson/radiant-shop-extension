- render_region :foot do |foot|
  - foot.buttons do
    %p.buttons{:style=>"clear: left"}
      = save_model_button(@shop_order)
      = save_model_and_continue_editing_button(@shop_order)
      = t('or')
      = link_to t('cancel'), admin_shop_orders_path
  - foot.timestamp do
    = updated_stamp @shop_order