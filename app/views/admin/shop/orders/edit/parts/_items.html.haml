%ul#items
  - @shop_order.line_items.each do |item|
    %li.item
      %h3= "#{item.quantity} (#{number_to_currency item.price})"
      = "#{item.item.name} (#{number_to_currency item.item.price})"
      